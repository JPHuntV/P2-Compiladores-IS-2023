

float miFunc (int dif, char otra){
	int otrasS = 1$
	int otrasSsd = 2+(3+4)$
	int prueba = test(1,2)$
	int prueba = tess()$
	int otras = -dif$
	int otrase = -2$
	String str = "Hola $%&/#$&) mundo"$
	int var$
	for(int i=0$i>var$++i)
	{
		do
		{
			int fvar = var-1$
		}while(var>12 # (34+34)>12)$

		if (var == 0)
		{
			escribir("entra al if")$
		}
		elif(true)
		{
			print(var)$
		}
		elif(false^true)
		{
			leer(var)$
			break$
		}
	}
	return 1.1$ @error
}

bool miOtraFun(){
	return true$ @error
}
int test(int a, int b){
	return a+b$
}
int main(){
	escribir("hola")$
	escribir(34)$
	escribir(6.7)$@solucionar negativos
	return 1$
}